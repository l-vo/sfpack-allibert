{% extends 'base.html.twig' %}

{% block title %}Movie details{% endblock %}

{% block body %}
<h1>{{ movie.title }}</h1>
<img src="{{ asset('build/images/' ~ movie.image) }}" alt="Image movie {{ movie.title }}" />
<ul>
    <li>released at: {{ movie.releasedAt|format_date }}</li>
    <li>genres: {{ movie.genres|join(', ') }}</li>
</ul>
{% endblock %}
